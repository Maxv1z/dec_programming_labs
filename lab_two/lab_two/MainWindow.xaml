<Window x:Class="FortuneTeller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FortuneTeller"
        Title="Гра Ворожка" Height="300" Width="400">
	<Window.Resources>
		<!-- Background gradient for a pleasant look -->
		<LinearGradientBrush x:Key="BackgroundBrush" StartPoint="0,0" EndPoint="1,1">
			<GradientStop Color="LightGoldenrodYellow" Offset="0"/>
			<GradientStop Color="LightSkyBlue" Offset="1"/>
		</LinearGradientBrush>
		<!-- Custom style for the button -->
		<Style x:Key="CustomButtonStyle" TargetType="Button">
			<Setter Property="Padding" Value="8"/>
			<Setter Property="Margin" Value="5"/>
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="BorderBrush" Value="Gray"/>
			<Setter Property="BorderThickness" Value="1"/>
		</Style>
	</Window.Resources>

	<Grid Background="{StaticResource BackgroundBrush}" Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Question input -->
		<StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,10">
			<Label Content="Введіть своє питання:" FontWeight="Bold"/>
			<TextBox x:Name="QuestionTextBox" Height="30"/>
		</StackPanel>

		<!-- Button with command binding -->
		<Button x:Name="GetAnswerButton"
                Grid.Row="1"
                Content="Дізнатись відповідь"
                Style="{StaticResource CustomButtonStyle}"
                Width="150"
                HorizontalAlignment="Left"
                Command="{x:Static local:MainWindow.GetAnswerCommand}"/>

		<!-- Display the generated answer -->
		<Label x:Name="AnswerLabel"
               Grid.Row="2"
               Content="Ваша відповідь з'явиться тут"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               FontSize="16"
               FontWeight="Bold"
               Margin="0,20,0,0"/>
	</Grid>
</Window>
